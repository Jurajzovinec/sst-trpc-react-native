{
  "name": "landing-page",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && vite build",
    "dev": "vite",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "dotenv": "^16.0.0"
  }
}